{
  "name": "pc-anatomy-web-experience",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run typecheck && vite build && npm run verify:dist",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "verify:dist": "node scripts/verify-dist.mjs",
    "test:e2e": "playwright test",
    "screenshots": "playwright test tests/e2e/w1.spec.ts",
    "validate": "npm run build && npm run test:e2e"
  },
  "dependencies": {
    "three": "0.180.0"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "@types/node": "^24.13.3",
    "@types/three": "0.180.0",
    "typescript": "5.9.2",
    "vite": "7.3.6"
  }
}
